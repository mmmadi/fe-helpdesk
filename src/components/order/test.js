{
  /* {underComments
                .filter((x) => x.comment_id === comment.id)
                .map((s) => (
                  <div
                    className="replies card"
                    style={{ marginLeft: "calc(1rem + 30px)" }}
                    key={s.id}
                  >
                    <div className="card-body">
                      <div className="media">
                        <div className="position-relative">
                          <img
                            src={DefUser}
                            alt="default user icon"
                            className="ui-w-40 rounded-circle"
                          />
                        </div>
                        <div className="media-body ml-3">
                          <div className="float-right text-muted small">
                            <FontAwesomeIcon icon={faClock} className="icon" />
                            {moment(s.time).fromNow()}
                          </div>
                          <span>{s.fio}</span>
                        </div>
                      </div>
                      <div
                        className="mt-2 comment-1"
                        style={{ marginLeft: "calc(1rem + 30px)" }}
                      >
                        <p></p>
                        <p>{s.text}</p>
                        <p></p>
                      </div>
                    </div>
                  </div>
                ))} */
}
